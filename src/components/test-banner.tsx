export function TestBadge() {
  return (
    <span className="animate-pulse-slow inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-[var(--amber)]/10 text-[var(--amber)] text-xs font-medium font-heading">
      <span className="w-1.5 h-1.5 rounded-full bg-[var(--amber)]" />
      Test Mode
    </span>
  );
}
